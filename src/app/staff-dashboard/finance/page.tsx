import AdminPlaceholder from "@/components/admin/AdminPlaceholder";

export default function AdminFinancePage() {
  return (
    <AdminPlaceholder
      title="Finance"
      icon="payments"
      blurb="Takings, deposits, and outstanding balances for the house — admin only."
    />
  );
}
