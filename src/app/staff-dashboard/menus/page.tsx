import AdminPlaceholder from "@/components/admin/AdminPlaceholder";

export default function AdminMenusPage() {
  return (
    <AdminPlaceholder
      title="Menus"
      icon="restaurant_menu"
      blurb="Update dishes, pricing, and collection items for the floor and kitchen."
    />
  );
}
