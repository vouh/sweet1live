import AdminPlaceholder from "@/components/admin/AdminPlaceholder";

export default function AdminEventsPage() {
  return (
    <AdminPlaceholder
      title="Events"
      icon="mic"
      blurb="Schedule live nights, capacity, and ticket holds without leaving the admin desk."
    />
  );
}
