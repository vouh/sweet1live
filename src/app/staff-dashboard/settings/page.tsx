import AdminPlaceholder from "@/components/admin/AdminPlaceholder";

export default function AdminSettingsPage() {
  return (
    <AdminPlaceholder
      title="Settings"
      icon="settings"
      blurb="House preferences, staff access, and operational defaults."
    />
  );
}
