import AdminPlaceholder from "@/components/admin/AdminPlaceholder";

export default function AdminReservationsPage() {
  return (
    <AdminPlaceholder
      title="Reservations"
      icon="event_seat"
      blurb="Manage covers, confirmations, and floor timing for tonight’s book — staff tools only."
    />
  );
}
